import React, { Component } from "react";

class MoveBackButton extends Component {
    render() {
        return (
            <button className="settings-button">
                Назад
            </button>
        );
    }
}

export default MoveBackButton;